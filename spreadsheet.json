{
    "name": "weather",
    "cells": {
        "1,0": "Paris",
        "2,0": "Nairobi",
        "3,0": "Tokyo",

        "0,1": "Current weather",
        "1,1": {
            "value": "=LAST(weather.item.condition.text)",
            "filter": "weather.location.city == \"Paris\""
        },
        "2,1": {
            "value": "=LAST(weather.item.condition.text)",
            "filter": "weather.location.city == \"Nairobi\""
        },
        "3,1": {
            "value": "=LAST(weather.item.condition.text)",
            "filter": "weather.location.city == \"Tokyo\""
        },

        "0,2": "Current temperature",
        "1,2": {
            "value": "=CONCATENATE(ROUND((LAST(weather.item.condition.temp) - 32) * 5 / 9, 0), \"°C\")",
            "filter": "weather.location.city == \"Paris\""
        },
        "2,2": {
            "value": "=CONCATENATE(ROUND((LAST(weather.item.condition.temp) - 32) * 5 / 9, 0), \"°C\")",
            "filter": "weather.location.city == \"Nairobi\""
        },
        "3,2": {
            "value": "=CONCATENATE(ROUND((LAST(weather.item.condition.temp) - 32) * 5 / 9, 0), \"°C\")",
            "filter": "weather.location.city == \"Tokyo\""
        },

        "0,3": "Average",
        "1,3": {
            "value": "=CONCATENATE(ROUND((AVG(weather.item.condition.temp) - 32) * 5 / 9, 0), \"°C\")",
            "filter": "weather.location.city == \"Paris\""
        },
        "2,3": {
            "value": "=CONCATENATE(ROUND((AVG(weather.item.condition.temp) - 32) * 5 / 9, 0), \"°C\")",
            "filter": "weather.location.city == \"Nairobi\""
        },
        "3,3": {
            "value": "=CONCATENATE(ROUND((AVG(weather.item.condition.temp) - 32) * 5 / 9, 0), \"°C\")",
            "filter": "weather.location.city == \"Tokyo\""
        },

        "0,4": "Record high",
        "1,4": {
            "value": "=CONCATENATE(ROUND((MAX(weather.item.condition.temp) - 32) * 5 / 9, 0), \"°C\")",
            "filter": "weather.location.city == \"Paris\""
        },
        "2,4": {
            "value": "=CONCATENATE(ROUND((MAX(weather.item.condition.temp) - 32) * 5 / 9, 0), \"°C\")",
            "filter": "weather.location.city == \"Nairobi\""
        },
        "3,4": {
            "value": "=CONCATENATE(ROUND((MAX(weather.item.condition.temp) - 32) * 5 / 9, 0), \"°C\")",
            "filter": "weather.location.city == \"Tokyo\""
        }
    }
}
